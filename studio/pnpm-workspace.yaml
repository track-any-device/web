# pnpm 11 moved build-script approval out of package.json's "pnpm" field into this file.
# esbuild ships a platform binary via its install script; Sanity's build/deploy needs it.
onlyBuiltDependencies:
  - esbuild
